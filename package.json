{
  "name": "@maxiscoding/npm-package-example",
  "version": "1.0.0",
  "author": "Maxim Zaytsev <max.zaytsev@team.bumble.com>",
  "license": "MIT",
  "main": "./dist/index.js",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup",
    "test": "echo \"Error: no test specified\" && exit 1",
    "ci:clean": "rm -rf node_modules && rm -rf package-lock.json && npm install"
  },
  "devDependencies": {
    "ts-node": "^10.9.2",
    "tsup": "^8.0.2",
    "typescript": "^5.3.3"
  }
}
